
interface PesquisaProps {
    colorScheme: string,

    onSubmit?: () => {}
}

export default function Pesquisa(props: PesquisaProps) {
    return (
        // Refazer com componentes nativo e tailwind
        <span>Input de pesquisa</span>
    )
}